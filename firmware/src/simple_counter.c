#include "simple_trng.h"
#include "host_messaging.h"

int counter_init() {

    print_debug("[DEBUG] Awakening the Counter module (Not really)"); // FIXME: Remove the not really after code is done

    return 0;
}